# Phase: review iter 1 — iterating

## review Output
{
  "verdict": "fail",
  "dimensions": [
    {
      "name": "Requirements Met",
      "score": 7,
      "gaps_to_10": "Rival battle triggers correctly via LOS, counter-starter species is correct, rival NPC blocks and unblocks as expected, re-trigger guard works. However: (1) the explicit task requirement that RIVAL_BATTLE_TRIGGER should 'transition to GamePhase.TRAINER_BATTLE_INTRO' is not satisfied — the script handler transitions to DIALOG only and no script zone references it anyway; (2) blackout (player loses) is not fully handled when lastPokemonCenter is undefined (game start state)."
    },
    {
      "name": "Error Handling",
      "score": 7,
      "gaps_to_10": "createPokemonInstance validation in handleRivalBattle is dead code (the method itself is never called). The live path in createTrainerBattleState has no try/catch around createPokemonInstance/buildPartyFromEntries — if a corrupted rivalStarterSpeciesId were stored, it would throw and crash the game loop. Not likely in practice but also not guarded."
    },
    {
      "name": "UX Completeness",
      "score": 6,
      "gaps_to_10": "Soft-lock when player loses the rival battle: lastPokemonCenter is undefined at game start (never set until the player loads the 'pokemon-center' map for the first time, which requires beating the rival to exit Pallet Town). If all party Pokemon faint, handleBattleDone falls through to state.phase = GamePhase.OVERWORLD with no heal and no warp. The player is permanently stuck in the lab with 0-HP Pokemon and no recovery path. This is the only known Pokemon Center, and the player cannot reach it without first walking through Route 1."
    },
    {
      "name": "Code Quality",
      "score": 7,
      "gaps_to_10": "handleRivalBattle is unreachable dead code (no script zone in pallet-town.json references RIVAL_BATTLE_TRIGGER). If it were called, it would show dialog but never start the battle because ctx.pendingTrainerBattle is never set. rival_battle_pending is never cleared to false via the LOS battle path (only through the dead-code script handler), leaving it permanently true after the rival is defeated — logically inconsistent even if harmless in practice due to trainer_defeated_rival. createPokemonInstance is called twice in the happy path (once wastefully for 'validation' in the dead code)."
    },
    {
      "name": "Test Coverage",
      "score": 7,
      "gaps_to_10": "Good coverage of LOS activation, counter-starter mapping, activationFlag NPC loading, and post-defeat collision bypass. Missing: (1) test for blackout/soft-lock when lastPokemonCenter is undefined and all Pokemon faint; (2) integration test that GamePhaseUpdater's pendingTrainerBattle flag causes DIALOG → TRANSITION after pre-battle dialog; (3) test confirming rival_battle_pending is cleared (it isn't via the LOS path)."
    },
    {
      "name": "Security",
      "score": 9,
      "gaps_to_10": "No security concerns — pure game-state logic, no user input flowing to external systems, no API keys."
    }
  ],
  "average_score": 7.2,
  "issues": [
    {
      "severity": "P1",
      "file": "src/game/engine/GamePhaseUpdater.ts",
      "description": "Soft-lock when player loses the rival battle before visiting any Pokemon Center. createInitialGameState() sets lastPokemonCenter to undefined. The rival battle occurs in Pallet Town before any Pokemon Center is accessible (Route 1 is blocked by the rival). If the player's party is wiped, handleBattleDone checks `allFainted && state.lastPokemonCenter` — this evaluates to false (undefined), falls through to `state.phase = GamePhase.OVERWORLD`, and the player is stranded in the lab with 0-HP Pokemon. There is no nurse or Pokemon Center in Pallet Town to recover from.",
      "fix_guidance": "Add a fallback default to lastPokemonCenter in createInitialGameState (e.g., the Pallet Town starting tile), so the player is always returned to a safe spawn point regardless of prior Pokemon Center visits. Alternatively, when lastPokemonCenter is undefined in handleBattleDone, reset the player to a hardcoded safe spawn (e.g., state.currentMap = 'pallet-town', player.tileX = 10, player.tileY = 10) and heal all Pokemon before setting phase to OVERWORLD.",
      "checklist_item": "ux"
    },
    {
      "severity": "P2",
      "file": "src/game/engine/ScriptHandler.ts",
      "description": "handleRivalBattle (the RIVAL_BATTLE_TRIGGER case) is dead code — no script zone in pallet-town.json references this scriptId, so it is never called. The rival battle is triggered exclusively via the TrainerNPC LOS approach system. Additionally, if it were called, it would be broken: the method sets state.phase = GamePhase.DIALOG without setting ctx.pendingTrainerBattle = true, so when the dialog ends the phase would return to OVERWORLD instead of starting the battle. The task requirement was for this script handler to 'transition to GamePhase.TRAINER_BATTLE_INTRO'.",
      "fix_guidance": "Either: (a) add a script zone in pallet-town.json at the lab exit tiles referencing RIVAL_BATTLE_TRIGGER and fix handleRivalBattle to set state.phase = GamePhase.TRAINER_BATTLE_INTRO (not DIALOG) after showing dialog via a pendingTrainerBattle flag mechanism, OR (b) remove the handleRivalBattle method entirely since the LOS path fully implements the required behaviour. If keeping the script, it must call ctx.pendingTrainerBattle = true before setting phase to DIALOG, but ctx is not currently passed to ScriptHandler — so a dedicated TRAINER_BATTLE_INTRO transition is simpler.",
      "checklist_item": "requirements"
    },
    {
      "severity": "P2",
      "file": "src/game/engine/ScriptHandler.ts",
      "description": "rival_battle_pending is never cleared to false through the LOS battle path (the only live path). It is only cleared inside handleRivalBattle which is dead code. After the rival battle completes, rival_battle_pending remains true indefinitely. The rival stays inactive because trainer_defeated_rival takes precedence in the loadFromMap defeated check, but any future code that reads rival_battle_pending to determine whether the rival has been encountered will see a stale true value.",
      "fix_guidance": "In handleBattleDone (GamePhaseUpdater.ts), after markDefeated is called for the rival NPC, also set state.flags['rival_battle_pending'] = false to keep the flag consistent with game reality.",
      "checklist_item": "requirements"
    },
    {
      "severity": "P2",
      "file": "tests/rival-battle.test.ts",
      "description": "No test covers the blackout/soft-lock path (player loses rival battle with lastPokemonCenter undefined). The test file has excellent coverage of the activation and LOS mechanics but leaves the loss path untested.",
      "fix_guidance": "Add a test that sets up a GameState where all partyPokemon have currentHp = 0 and lastPokemonCenter is undefined, calls handleBattleDone logic, and asserts the player is warped to a safe default location (or at minimum that the game does not get stuck). Also add a test verifying that rival_battle_pending is false after the rival is marked defeated.",
      "checklist_item": "test_quality"
    }
  ],
  "summary": "The core rival battle flow is correctly implemented and works end-to-end via the TrainerNPC LOS system: the player picks a starter, rival_battle_pending is set, the rival NPC becomes active, the LOS approach triggers TRAINER_BATTLE_INTRO, the counter-starter species is read from flags, preBattleDialog is shown, and after winning the rival is permanently defeated so the player can exit. All 289 tests pass. The single P1 failure is a soft-lock: lastPokemonCenter is undefined at game start and the player cannot reach any Pokemon Center before fighting the rival (Route 1 requires passing the rival), so losing the battle leaves the player stranded in the lab with fainted Pokemon and no recovery path. Secondary issues are dead code in the RIVAL_BATTLE_TRIGGER script handler (which would also fail to start a battle if triggered) and a stale rival_battle_pending flag after defeat."
}

