# Phase: review iter 1 — converged

## review Output
{
  "verdict": "pass",
  "dimensions": [
    {
      "name": "Requirements Met",
      "score": 8,
      "gaps_to_10": "Performance verification (heap growth, <5 frames over budget) required by spec is not tested in E2E tests — the frame-time tracker logs console warnings but no test asserts the budget-exceeded count. Post-blackout dialog ('You blacked out!') specified in the plan was not implemented; the game silently heals and warps. Text speed setting in OPTIONS menu is not wired to DialogSystem (always uses NORMAL=40ms/char regardless of player setting), so the '3-minute FAST text speed' benchmark cannot be verified. All 4 E2E journey tests, integration checks, and core polish items are otherwise present."
    },
    {
      "name": "Error Handling",
      "score": 9,
      "gaps_to_10": "All critical paths are covered: save/load wrapped in try/catch with user-visible StorageWarning banner, missing sprites produce colored fallback, encounters wrapped in try/catch returning null, and the main game loop catches any unhandled exception and renders an error overlay. Minor gap: AssetLoader.loadImage rejects (not resolves) on 404, so callers must catch — all call sites do, but the reject-based API is slightly dangerous for future code."
    },
    {
      "name": "UX Completeness",
      "score": 7,
      "gaps_to_10": "Post-blackout dialog not shown (silent heal/warp is jarring). handleConfirm + onChoice + startDialog race: when a choice dialog calls dialog.startDialog() inside onChoice(), the subsequent this.currentPage++ in handleConfirm() advances past page 0 of the new dialog, skipping the first confirmation message (e.g., 'CHARMANDER chose you!' is never shown). OPTIONS menu renders hardcoded settings (textSpeed: 'MID') regardless of actual player setting. These are polish gaps but do not crash or block gameplay."
    },
    {
      "name": "Code Quality",
      "score": 8,
      "gaps_to_10": "GameEngine.ts is exactly 199 lines (1 under the 200-line limit — extremely tight margin). All previously over-200-line files were correctly split. Dead-code ternary in GamePhaseUpdater.ts line 72: `const badgeIndex = npcEntity.badgeReward === 'BOULDER' ? 0 : 0` — both branches return 0, so all badge types incorrectly map to slot 0; functionally harmless now (only BOULDER exists) but will cause silent bugs when more badges are added. The MapBundles import path uses a relative maps subdirectory (correct). The split into GamePhaseUpdater/GamePhaseRenderer/ScriptHandler is clean and readable."
    },
    {
      "name": "Test Coverage",
      "score": 7,
      "gaps_to_10": "All 4 required journey specs and integration-checks spec are present and well-structured. Key gaps: (1) No performance E2E test (heap growth, frame budget count) despite being required. (2) gym-badge journey test title claims 'badge in localStorage' but only verifies phase, not localStorage badge state. (3) No negative path test for blackout/warp scenario. (4) Wild-catch test final assertion `expect(finalPartyCount).toBeGreaterThanOrEqual(initialPartyCount)` is so tolerant (same count acceptable) it would pass even if the catch completely fails — the catch may fail since it's random; a seeded low-HP wild Pokemon would improve reliability. (5) New-game journey starter-selection test relies on implicit behavior (player already in trigger zone causes immediate dialog), which is correct but undocumented and fragile."
    },
    {
      "name": "Security",
      "score": 10,
      "gaps_to_10": "No hardcoded API keys, secrets, or credentials found in any source file. The integration-checks test actively scans for known secret patterns (sk-*, OPENAI_API_KEY, Bearer token). No server-side calls, no user-generated content stored externally. Name entry is limited to 7 chars by DialogSystem.maxNameLen. localStorage keys are well-scoped. No XSS surface (canvas-based rendering, not innerHTML)."
    }
  ],
  "average_score": 8.17,
  "issues": [
    {
      "severity": "P2",
      "file": "src/game/engine/GamePhaseUpdater.ts",
      "description": "Post-blackout dialog not shown. When all party Pokemon faint and lastPokemonCenter exists, the game silently heals and warps without displaying 'You blacked out!' or 'Your Pokemon were healed to full health!' dialogs. The plan explicitly required this in step 9.",
      "fix_guidance": "In handleBattleDone(), before setting state.phase = GamePhase.OVERWORLD for the allFainted path, call deps.dialog.startDialog with blackout/heal messages and set state.phase = GamePhase.DIALOG. Only set OVERWORLD after dialog completes (the existing dialog completion check in GamePhaseUpdater handles the transition).",
      "checklist_item": "ux"
    },
    {
      "severity": "P2",
      "file": "src/game/systems/DialogSystem.ts",
      "description": "handleConfirm() has a race condition when onChoice() calls startDialog() internally. After onChoice() executes (which calls startDialog() to reset pages/currentPage to 0), handleConfirm() then increments this.currentPage from 0 to 1, skipping the first page of the newly started dialog. For the starter selection, 'CHARMANDER chose you!' is never displayed — the player sees 'Take good care of it!' immediately.",
      "fix_guidance": "Track whether startDialog was called inside onChoice by storing a flag or checking if this.currentPage was reset. If onChoice triggered a startDialog, do NOT increment this.currentPage afterward. Alternatively, have startDialog set a sentinel boolean that handleConfirm checks before the currentPage++ step.",
      "checklist_item": "ux"
    },
    {
      "severity": "P2",
      "file": "src/game/systems/MenuSystem.ts",
      "description": "Text speed setting in OPTIONS view is hardcoded to { textSpeed: 'MID', sound: 'MONO', scale: '2X' } and is never read from or written to any persistent state. Changing the setting in OPTIONS does nothing — the DialogSystem always uses NORMAL (40ms/char) speed. The plan's step 13 required wiring the OPTIONS textSpeed to DialogSystem.",
      "fix_guidance": "Add a module-level singleton or React context bridge to expose the player's textSpeed setting to the game engine. In MenuSystem.handleOptionsView(), update a shared mutable settings object when ArrowLeft/Right changes textSpeed. In GameEngine or GamePhaseUpdater, read this setting and pass it as the `speed` parameter to dialog.startDialog() calls.",
      "checklist_item": "requirements"
    },
    {
      "severity": "P2",
      "file": "src/game/engine/GamePhaseUpdater.ts",
      "description": "Dead-code ternary at line 72: `const badgeIndex = npcEntity.badgeReward === 'BOULDER' ? 0 : 0`. Both branches evaluate to 0, meaning all badge types will map to badge slot 0. Functionally correct only because BOULDER is the only badge currently used.",
      "fix_guidance": "Replace with a lookup table: `const BADGE_INDEX: Record<string, number> = { BOULDER: 0, CASCADE: 1, THUNDER: 2, RAINBOW: 3, SOUL: 4, MARSH: 5, VOLCANO: 6, EARTH: 7 }` and use `const badgeIndex = BADGE_INDEX[npcEntity.badgeReward] ?? 0`.",
      "checklist_item": "architecture"
    },
    {
      "severity": "P2",
      "file": "tests/e2e/integration-checks.spec.ts",
      "description": "Performance verification (heap growth measurement, assertion that <5 frames exceeded budget) is not implemented in any E2E test. The spec requires verifying '<5 frames over budget' and 'heap growth' but the integration-checks suite has no such test. The frame-time tracker in GameEngine only logs console.warn — it is not verified.",
      "fix_guidance": "Add a performance test to integration-checks.spec.ts: use page.evaluate to play through a 10-second overworld walk, then access window.__GAME_DEBUG__.engine to read a frame-budget-exceeded counter that should be tracked in GameEngine. For heap growth, use Playwright's CDP session to call Performance.getMetrics before and after gameplay and assert heapUsed growth is reasonable.",
      "checklist_item": "test_quality"
    },
    {
      "severity": "P2",
      "file": "tests/e2e/gym-badge-journey.spec.ts",
      "description": "The test named 'complete Brock battle -> badge ceremony -> badge in localStorage' does not verify the localStorage badge. The assertion only checks that phase is 'BADGE_CEREMONY' or 'OVERWORLD', not that localStorage contains the badge state. The test title is misleading and the badge persistence requirement is untested.",
      "fix_guidance": "After the phase check, add: `const saveRaw = await page.evaluate(() => localStorage.getItem('pokebrowser_save_v1')); if (saveRaw) { const save = JSON.parse(saveRaw); const slot1 = save['1']; if (slot1) expect(slot1.badges?.[0]).toBe(true); }` — but also trigger a save first (press Enter -> SAVE -> Z -> Z) before the reload check.",
      "checklist_item": "test_quality"
    }
  ],
  "summary": "The implementation successfully delivers all 4 Playwright E2E journey tests, integration-checks suite, the cross-cutting polish items, and the production-readiness requirements. All TypeScript source files (excluding test files) are under 200 lines; the critical GameEngine.ts refactor lands at exactly 199 lines. The save/load unification (applyV1Save reading from systems/SaveSystem.load(1)) correctly restores party/inventory/flags/position after browser reload. The ScriptHandler properly implements STARTER_TABLE_TRIGGER with choice dialog. BattleSystem correctly handles the ITEM (Pokeball) action through the full catch cycle. The frame-time tracker (console.warn when dt>20ms) and RAF cleanup are both in place. No hardcoded secrets exist and the security scan in integration-checks is active. Unit tests (215) all pass and TypeScript compiles without errors. The six P2 issues found are: (1) missing post-blackout dialog, (2) handleConfirm+onChoice+startDialog race skipping the first page of nested dialogs, (3) text speed setting not wired from OPTIONS to DialogSystem, (4) dead-code ternary for badge index mapping, (5) no E2E performance verification test, and (6) gym-badge test misleadingly titled without actually verifying localStorage badge persistence. None of these are user-blocking bugs, and no P1 issues were found. Verdict: PASS."
}

